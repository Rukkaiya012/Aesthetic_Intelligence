# Project Setup

1. Create a folder in your C drive with Aesthetic_Intelligence name 
2. Create all JS files (`behaviour_engine.js`, `pattern_detector.js`, `layout_engine.js`, `sync_core_stub.js`) , index.html and style.css files
3. Open `index.html` in browser
4. Open console to see behaviour logs
5. Interact with panels and buttons to test
